name: Publicar
on: push

jobs:
  PsGallery:
    name: PsGallery 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
            fetch-tags: true
      - name: Publish
        shell: pwsh
        run: ./publish.ps1